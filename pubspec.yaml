name: flutter_app
description: Curso desarrollo de aplicaciones moviles
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.1.5 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  http: any

dev_dependencies:
  dart_code_metrics: ^4.5.0
  
  flutter_lints: ^2.0.0
 
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
